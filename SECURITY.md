# 安全策略

## 报告安全问题

请不要在公开 Issue 中提交漏洞细节、真实服务器地址、员工数据、激活码、API Key、日志或配置文件。

维护者在公开仓库上线前应配置一个私密安全报告渠道，并在本节补充具体入口。收到报告后，维护者应先确认影响范围，再协调修复和披露时间。

## 部署方责任

本项目提供企业内部 Harness 的参考实现，不替代企业自身的安全设计。生产部署至少应完成：

- 为所有数据库、网关、管理账号和激活哈希使用独立随机凭据；
- 使用企业域名、HTTPS 和受信任证书；
- 限制管理入口来源，不向员工网络公开 LiteLLM 管理 API；
- 使用低权限部署账号，并限制 Docker、文件和网络权限；
- 加密备份并验证恢复流程；
- 对 Windows 安装包进行 Authenticode 签名；
- 在升级前审查 DeepSeek Harness、LiteLLM、Node.js、WebView2 和其他依赖的安全公告。

## 敏感数据

仓库不得包含 `.env`、私钥、证书密码、真实 API Key、员工数据、生产日志、数据库转储、真实服务器地址或内部备份标识。提交前运行：

```powershell
pwsh ./scripts/verify-public-release.ps1
```
