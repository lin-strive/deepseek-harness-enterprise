# 超智能 Harness 项目生命周期

> 文档状态：已确认，作为项目范围与交付基线
> 适用平台：Windows 10/11 x64
> 当前阶段：M1 技术验证 / Windows 桌面壳最小闭环
> 最后更新：2026-09-14

## 1. 文档目的

本文档用于管理超智能 Harness 从需求确认、设计、开发、测试、发布、运维、上游升级到退役的完整生命周期。

所有需求变更、架构调整、版本发布和风险接受均应更新本文档或在本文档中登记对应记录。开发实现不得偏离“已确认决策”；确需变更时，必须先完成变更评审。

## 2. 项目目标

在不修改 DeepSeek Harness 上游源码的前提下，交付一套公司内部使用的 Windows AI 开发工具，包含：

- Windows 桌面安装包与公司桌面壳；
- 内置、固定版本的 DeepSeek Harness 与 Node.js Runtime；
- 本地代码工作区与本地会话；
- 公司统一模型网关；
- 员工激活、虚拟 API Key、额度和权限管理；
- 公司 Skills 与策略包；
- 可审计、可灰度、可回滚的更新机制。

项目最终用户只需要安装公司提供的 EXE、输入激活码并选择本地工作区，不需要安装 Node.js，不需要持有公司真实的 DeepSeek API Key。

## 3. 已确认决策

以下决策已经冻结，除非通过正式变更流程，不得在实现中改变：

| 编号 | 决策 |
|---|---|
| D-001 | Harness 在员工 Windows 电脑本地运行，不集中运行用户 Harness 容器。 |
| D-002 | 第一阶段仅支持 Windows 10/11 x64。 |
| D-003 | 不修改、不 Fork DeepSeek Harness 上游源码。 |
| D-004 | 公司版本统一使用公司模型网关，不提供个人 Provider 配置入口。 |
| D-005 | 员工客户端不得保存公司真实 DeepSeek API Key，只保存个人虚拟 Key。 |
| D-006 | 激活不绑定设备，不采集硬件指纹。 |
| D-007 | 激活码一次性使用；重装或换电脑时由管理员重新签发。 |
| D-008 | 输入有效激活码后，先显示姓名、员工编号、部门，用户确认后才完成激活。 |
| D-009 | 网关不记录提示词、代码内容或模型完整回复。 |
| D-010 | 额度同时支持月度金额、RPM、TPM和最大并发请求数。 |
| D-011 | 公司 Skills 与策略包纳入统一发布和更新。 |
| D-012 | 客户端提供“检查更新”按钮，并支持后台周期检查。 |
| D-013 | 客户端只安装公司测试并批准的 Harness 版本，不直接自动安装 GitHub/npm 最新版。 |
| D-014 | 模型网关优先采用 LiteLLM Proxy + PostgreSQL。 |
| D-015 | 自研服务只承担员工、激活、发布和 LiteLLM 管理编排，不重新实现模型数据面。 |
| D-016 | 月度额度默认使用人民币，并按 Asia/Shanghai 时区在每月 1 日重置。 |
| D-017 | 保留 Harness 内置 `web-search-deepseek`，将其 `baseURL` 指向公司 LiteLLM `/v1`、模型设为 `company-fast`；不开发自定义搜索插件，不允许搜索请求直连官方端点。 |
| D-018 | 激活码默认 24 小时，支持 1 小时、8 小时、1 天、3 天、7 天、30 天和最长 90 天的自定义到期时间；员工访问期限与激活码期限分离，可长期有效至手动禁用，或指定结束时间。 |
| D-019 | Windows 客户端使用 48px 单层公司标题栏；默认跟随 Harness 浅深主题，保留跟随 Windows/固定浅色/固定深色选项；重启与后续更新入口归入员工菜单，且不修改 Harness 上游源码。 |
| D-020 | Windows 客户端采用当前用户安装模式，示例品牌默认安装到 `%LOCALAPPDATA%\Programs\SmartWheelchairHarness`，不要求管理员权限；每个 Windows 用户只运行一个实例。 |
| D-021 | 安装包包含自包含 .NET、固定 Node.js 和固定 Harness Runtime；WebView2 使用微软 Evergreen Bootstrapper，仅在缺失时安装，并在构建时验证微软数字签名。 |
| D-022 | 卸载默认保留员工激活、会话和设置；仅在非静默卸载中由员工明确选择后，才清除固定应用数据目录和 Credential Manager 虚拟 Key。 |
| D-023 | 开源仓库使用虚构企业品牌模板；每个实际企业版本必须使用独立安装 `AppId`、数据目录、凭据目标、Java 包名和激活码前缀，避免不同品牌间覆盖或串读数据。 |

## 4. 核心原则

### 4.1 上游零侵入

- 通过官方发布包或 npm 精确版本集成 DeepSeek Harness；
- 公司壳不直接修改上游源码、构建产物或数据库；
- 公司配置、Skills、品牌资源和更新逻辑位于独立目录；
- 每次升级通过替换 Runtime 完成，用户数据与 Runtime 分离；
- 禁止在生产中使用浮动的 `latest` 版本。

### 4.2 密钥最小暴露

- 真实模型供应商 Key 仅存在于网关服务器的密钥环境中；
- LiteLLM Master Key 仅允许管理服务访问；
- 员工获得独立、可吊销、有限额的虚拟 Key；
- Windows 客户端使用 Windows Credential Manager 保存虚拟 Key；
- 安装包、配置模板、日志和崩溃报告不得包含任何真实密钥。

### 4.3 本地数据优先

- 工作区、会话和个人配置默认保存在员工本机；
- 网关只记录调用元数据、Token、费用、耗时和状态；
- 不采集提示词、源代码、文件内容或模型回复正文；
- 诊断日志必须脱敏。

### 4.4 先验证再发布

- 上游新版本先进入候选环境；
- 自动化测试通过后进行小范围灰度；
- 灰度验证通过后才允许全量发布；
- 所有更新包必须具备哈希校验、数字签名和回滚能力。

## 5. 系统架构

```text
┌──────────────────────────────────────────────┐
│ 员工 Windows 电脑                            │
│                                              │
│ 超智能 Harness.exe                            │
│ ├─ WebView2 桌面壳                           │
│ ├─ 官方 DeepSeek Harness Runtime             │
│ ├─ Node.js Runtime                           │
│ ├─ Windows Credential Manager：员工虚拟 Key │
│ ├─ 本地工作区、会话、日志                    │
│ └─ 公司 Skills 与策略包                      │
└───────────────────┬──────────────────────────┘
                    │ HTTP（当前测试）/ HTTPS（正式）+ 员工虚拟 Key
                    ▼
┌──────────────────────────────────────────────┐
│ 公司模型网关                                 │
│ ├─ LiteLLM Proxy                             │
│ ├─ PostgreSQL                                │
│ ├─ 可选 Redis：分布式限流                    │
│ └─ DeepSeek 上游 Key                         │
└───────────────────┬──────────────────────────┘
                    │ HTTPS
                    ▼
             DeepSeek 官方 API

┌──────────────────────────────────────────────┐
│ 公司管理服务                                 │
│ ├─ 管理后台                                  │
│ ├─ 员工与部门                                │
│ ├─ 激活码                                    │
│ ├─ LiteLLM 管理 API 编排                     │
│ ├─ 额度与使用统计                            │
│ └─ 客户端、Harness、Skills 发布              │
└──────────────────────────────────────────────┘
```

## 6. 产品模块

### 6.1 Windows 桌面壳

建议技术栈：.NET 8、WPF 或 WinUI、WebView2、Inno Setup 或 WiX。

必须具备：

- 单实例启动；
- 内置 Node.js 和固定版本 Harness；
- 后台启动 Harness，不显示命令行窗口；
- Harness 仅监听本机回环地址；
- 自动选择未占用端口；
- 启动健康检查与超时提示；
- WebView2 内嵌 Harness 页面；
- 单层自绘标题栏，并保留 Windows 原生拖动、缩放、贴靠布局和系统菜单能力；
- 标题栏默认跟随 Harness 浅色/深色主题，允许跟随 Windows 或固定外观；
- 工作区选择；
- 托盘打开、重启、查看日志、退出；
- 进程异常退出检测和一键重启；
- 首次激活；
- 当前员工信息和额度展示；
- 当前桌面壳、Harness、Skills 版本展示；
- 手动“检查更新”；
- 每日后台检查一次更新；
- 下载进度、签名与哈希校验；
- 更新失败自动回滚；
- 卸载时默认保留用户数据。

### 6.2 激活服务

激活流程：

```text
管理员创建或导入员工
        ↓
生成一次性激活码
        ↓
员工输入激活码
        ↓
POST /activation/preview
        ↓
显示姓名、员工编号、部门
        ↓
员工确认
        ↓
POST /activation/confirm
        ↓
创建/分配 LiteLLM 虚拟 Key
        ↓
客户端保存虚拟 Key 和员工资料
```

规则：

- 激活码使用密码学安全随机数生成；
- 数据库只保存激活码哈希；
- 默认有效期 24 小时；
- 管理端提供 1 小时、8 小时、1 天、3 天、7 天、30 天预设和自定义到期时间，自定义最长 90 天；
- 激活码不提供一年或长期有效选项；员工访问期限单独管理；
- 只能成功兑换一次；
- Preview 接口只在激活码有效时返回姓名；
- Preview 和 Confirm 均需限流和失败次数限制；
- Confirm 必须使用短期激活会话，避免直接重复提交激活码；
- 激活码兑换必须使用数据库原子事务；
- 不记录设备指纹；
- 重装或换电脑由管理员重新签发激活码；
- 员工姓名、编号、部门不允许在客户端修改；
- 员工停用后，所有关联虚拟 Key 立即禁用。
- 员工访问期限到期后，ControlPlane 在一分钟内自动停用员工、撤销全部活跃虚拟 Key 并作废未使用激活码；
- 正式员工可设为长期有效至手动禁用，临时员工可指定北京时间的访问结束时间；
- 激活码到期时间不得晚于对应员工访问结束时间。

建议接口：

| 方法 | 路径 | 用途 |
|---|---|---|
| POST | `/activation/preview` | 验证激活码并返回待确认员工资料。 |
| POST | `/activation/confirm` | 确认身份并兑换虚拟 Key。 |
| GET | `/client/profile` | 刷新员工资料、账号状态和额度摘要。 |
| POST | `/client/deactivate` | 删除本机激活状态，不自动吊销服务端 Key。 |

### 6.3 LiteLLM 模型网关

必须具备：

- OpenAI-compatible API；
- DeepSeek Provider；
- SSE 流式响应；
- Tool Calls；
- 每员工独立虚拟 Key；
- 模型白名单；
- 月度金额预算；
- RPM；
- TPM；
- 最大并发请求数；
- Key 有效期、禁用和轮换；
- 用户、部门和模型维度的使用统计；
- 上游超时、错误透传与受控重试；
- 管理接口与模型调用接口分离；
- 真实上游 Key 定期轮换。

模型对客户端使用稳定别名，例如：

- `company-fast`
- `company-coder`
- `company-pro`

客户端不得依赖上游真实模型名称。管理员可以在不升级客户端的情况下调整别名映射。

### 6.4 公司管理后台

必须具备：

- 管理员本地账号密码；
- TOTP 二次验证；
- 员工、部门新增、编辑、停用；
- 员工访问期限设置、到期状态展示和自动停用；
- CSV 批量导入；
- 一次性激活码生成、重置、吊销；
- 虚拟 Key 创建、禁用、轮换；
- 模型权限；
- 个人与部门额度；
- RPM、TPM、并发数设置；
- 每日、每月用量统计；
- 金额、Token、延迟、错误率统计；
- CSV/Excel 导出；
- 客户端、Harness、Skills 版本管理；
- 候选、灰度、稳定发布通道；
- 管理员操作审计日志；
- 员工离职一键停用。

管理服务不得直接修改 LiteLLM 数据表，应使用 LiteLLM 管理 API。

### 6.5 公司 Skills 与策略包

第一阶段至少包含：

- 公司通用开发规范；
- Git 提交与分支规范；
- 安全开发规则；
- 代码评审检查表；
- 公司项目根目录约定；
- 默认 Agent 权限策略；
- 默认模型别名；
- 插件允许清单；
- Skills 版本清单和完整性哈希。

公司 Skills、员工个人 Skills 和项目 Skills 必须使用不同目录，升级公司包时不得覆盖个人或项目内容。

### 6.6 更新中心

更新对象分为：

| 对象 | 更新方式 | 是否需要重启 |
|---|---|---|
| 桌面壳 | 安装器或差分包 | 是 |
| Harness Runtime | 替换固定版本 Runtime | 是 |
| Node.js Runtime | 随 Harness 兼容性更新 | 是 |
| 公司 Skills | 签名资源包 | 通常否 |
| 公司策略与模型别名 | 签名配置清单 | 通常否 |

客户端更新清单至少包含：

- 桌面壳版本；
- Harness 版本；
- Node.js 版本；
- Skills 版本；
- 最低允许版本；
- 是否强制升级；
- 强制升级截止时间；
- 更新说明；
- 下载地址；
- 文件大小；
- SHA-256；
- 数字签名；
- 回滚版本。

## 7. 非目标范围

以下内容不进入首个正式版本：

- macOS、Linux、Windows ARM；
- SSO、LDAP、企业微信登录；
- 设备指纹和硬件绑定；
- 云端同步 Harness 会话；
- 员工自助注册或充值；
- 提示词、代码和模型回复正文审计；
- 修改 Harness 上游原生页面源码；
- 对公众提供模型 API；
- Kubernetes 部署；
- 自研模型流式代理；
- 复杂多供应商智能路由；
- 移动端客户端。

## 8. 开发阶段与里程碑

### M0：需求与架构基线

状态：交付物已输出，等待评审

交付物：

- [x] 确认部署方式为员工电脑本地运行；
- [x] 确认公司网关、虚拟 Key和额度策略；
- [x] 确认 Windows 首发；
- [x] 确认零侵入上游策略；
- [x] 确认激活姓名预览；
- [x] 确认客户端更新按钮；
- [x] 创建本生命周期文档；
- [x] 输出仓库目录结构；
- [x] 输出威胁模型；
- [x] 输出接口详细设计；
- [x] 输出数据库详细设计；
- [x] 输出 UI 页面与状态清单；
- [x] 输出技术选型基线。

退出条件：所有架构、数据、接口和安全边界完成评审，MVP 范围冻结。

### M1：技术验证

交付物：

- [x] LiteLLM + PostgreSQL 可重复部署；
- [x] DeepSeek 上游调用与流式响应验证；
- [x] Harness 通过公司网关完成一次工具调用；
- [x] 虚拟 Key、模型权限和四类额度验证；
- [x] Windows 壳可启动并关闭固定版本 Harness；
- [x] WebView2 正常加载，并可在启动探测失败后重试恢复；
- [x] 单层标题栏、员工菜单和 Harness 浅深主题联动；
- [ ] WebView2 页面刷新与渲染进程异常恢复；
- [x] Harness 用户数据与 Runtime 分离；
- [ ] 公司 Skills 加载验证；
- [ ] 更新包替换与回滚验证。

退出条件：关键技术路径无阻塞，形成可演示的端到端 PoC。

#### M1 当前验证记录（2026-09-04）

- .NET 解决方案 Debug 构建通过，0 警告、0 错误；
- .NET 客户端 19 项核心测试和 Spring Boot ControlPlane 3 项测试全部通过；
- 开发激活 API 的 Preview → 姓名确认 → Confirm 闭环通过；
- LiteLLM、PostgreSQL、Redis Compose 与 YAML 语法检查通过；
- 已在远端 `/opt/company-harness/litellm` 建立仅含部署文件的 Docker Compose 环境，不再同步项目源码；
- LiteLLM `v1.94.1` 与 PostgreSQL `16.10-alpine` 容器均通过健康检查；
- DeepSeek 上游 Key 已配置，`company-fast`、`company-coder`、`company-pro` 三个稳定别名可用；
- 自动化网关验收共 14 项：普通响应、SSE 流式、Tool Calls、模型白名单、RPM、TPM、预算、最大并发、禁用、日志与数据库正文泄露检查全部通过；
- `/v1/messages` 原生联网搜索兼容性验证通过，返回 `web_search_tool_result` 和 10 个来源 URL；无需开发公司搜索插件；
- 联网搜索测试正文未进入 LiteLLM 容器日志或 PostgreSQL，临时搜索虚拟 Key 已删除；
- 验收生成的临时虚拟 Key 已全部删除，服务器无 `m1-` 测试 Key 残留；
- DeepSeek 思考模式不接受强制 `tool_choice`，Harness 联调应允许模型自主选择工具；
- 远端 `.env` 权限为 `600`；宿主机 `8765` 按当前测试要求暂用 HTTP，接入真实员工前必须配置来源访问控制、正式域名和受信任证书；
- 已按官方公开入口固定启动参数：`dsh web --no-open --host 127.0.0.1 --port <port>`；
- 固定 Runtime 已成功构建为约 298 MB 的可迁移目录，迁移后本地 Web 启动、聊天和联网搜索均通过；
- 已实现 WPF + WebView2 桌面壳首版，包含公司品牌、激活码、员工身份确认、Credential Manager、Runtime 定位和 Harness 生命周期；
- 编译后的 Windows EXE 已完成真实窗口验收：默认焦点、无效码错误态、支持编号和“演示员工 / SW0001 / 研发部”身份页均通过；
- 已通过 SSH 本地隧道完成真实激活闭环：确认激活后由控制面调用远端 LiteLLM 签发员工虚拟 Key，客户端凭据成功访问三个公司模型；
- 使用该虚拟 Key 完成 `company-fast` 真实模型请求，响应与用量字段正常；
- 桌面壳从隔离的 Credential Manager 目标读取虚拟 Key，冷启动固定 Runtime，并在 WebView2 中加载 Harness 工作台；
- 修复首个健康请求超时被误判为整体启动超时的问题，并增加单次请求超时后继续探测的回归测试；
- 关闭桌面壳后 Harness Node 子进程正常退出；测试虚拟 Key 已吊销并验证返回 HTTP 401，本机测试凭据与激活资料已删除；
- 生产接入仍需将当前 HTTP 测试入口升级为正式域名 HTTPS，并增加来源访问控制。
- 控制面已迁移为 Java 21 + Spring Boot 4.1.1，使用 Flyway 管理既有 `company_harness` schema；旧 ASP.NET ControlPlane 已退出代码库和部署目录；
- Nginx 在公网 `8765` 仅转发激活 API 和 LiteLLM `/v1`；`/key/*` 与管理 API 不公开；
- Vue 管理网页和管理 API 由独立端口 `8766` 提供；当前测试期按要求临时公网开放，支持单人添加、受限删除、启用/禁用、CSV 导入、月额度/RPM/TPM/并发和批量激活码；
- 管理接口认证、员工增删与状态切换、CSV 导入、激活码生成、姓名预览、Confirm 签发、禁用联动 Key 撤销、公司模型鉴权和真实浏览器页面均通过验收；临时员工和 Key 已清理；
- Windows 客户端开发环境默认连接 `http://127.0.0.1:8765`；正式部署通过环境变量配置企业 HTTPS 地址；
- 远端控制面已切换到 Production 模式和独立 `company_harness` PostgreSQL 数据库/schema；激活码与预览会话只保存哈希，状态跨容器重启保留；
- 真实并发兑换测试得到一个 HTTP 200 和一个 HTTP 409，证明同一激活码只能签发一次；验收 Key 随后已从 LiteLLM 删除并在控制库标记停用；
- 新数据卷具备独立数据库初始化脚本；既有环境升级前须将 PostgreSQL 逻辑备份保存到部署目录外的受控备份位置。
- Spring Boot 迁移前须备份数据库、旧配置和旧部署输入，并完成可恢复性验证。
- 激活码有效期已改为 1 小时、8 小时、1 天、3 天、7 天、30 天和最长 90 天自定义北京时间；旧版 `expiresInHours` 接口仍通过回归测试。
- 员工访问期限支持长期或指定结束时间，Flyway V2 已在远端完成迁移；真实到期测试确认员工被自动禁用、LiteLLM Key 从 HTTP 200 变为 401，测试数据已清理。
- 有效期功能部署前须建立版本化备份和明确的回滚点。
- Windows 桌面壳移除原 64px 公司页头，改为 48px 单层标题栏；员工姓名、运行状态、主题策略和重启入口完成收拢，Harness 可用面积增加 64px；
- 主题桥仅从本地 WebView2 页面上报 `light`/`dark`，不读取或传输内容，仍保持对上游 Harness 零侵入；
- 新增外观偏好原子存储及 2 项回归测试；.NET 核心测试更新为 14/14，通过桌面项目独立构建（0 警告、0 错误）。
- 新增按 Windows 用户隔离的命名互斥量和激活事件；真实双进程验证中第二实例以退出码 0 结束，系统仅保留一个窗口；
- 新增 Inno Setup 安装定义及自包含 Release 打包脚本；安装源包含 .NET、固定 Node/Harness、公司策略和经微软签名验证的 WebView2 Bootstrapper，并排除 Runtime 验收测试目录和日志；
- 当前 Windows 安装源版本为 `0.2.1`，Node `24.20.0`，Harness `0.1.1-rc.2`；发布目录、重复启动及真实安装目录均通过冒烟验证；
- .NET 核心测试更新为 19/19；0.2.1 安装 EXE 与 SHA-256 元数据已生成，代码签名仍未接入。

### M2：基础设施与网关

交付物：

- [ ] 生产候选 LiteLLM 配置；
- [ ] PostgreSQL 备份与恢复；
- [ ] TLS、域名和网络访问控制；
- [ ] 上游 Key 密钥管理；
- [ ] 虚拟 Key 生命周期；
- [ ] RPM、TPM、金额和并发限制；
- [ ] 指标、日志和告警；
- [ ] 不记录内容的日志验证；
- [ ] 灾难恢复演练。

退出条件：网关达到内部试用所需的安全性和可恢复性。

### M3：管理服务

交付物：

- [ ] 管理员登录和 TOTP；
- [x] 员工、部门和 CSV 导入；
- [x] 激活码 Preview/Confirm；
- [x] LiteLLM 用户与虚拟 Key 编排；
- [x] 额度和模型权限；
- [ ] 用量查询与导出；
- [x] 员工停用与访问期限自动到期；
- [ ] 管理操作审计；
- [ ] 版本和更新清单管理。

退出条件：管理员无需操作数据库或服务器命令即可完成日常管理。

### M4：Windows 客户端

交付物：

- [x] 桌面壳与每 Windows 用户单实例；
- [ ] 托盘菜单与后台驻留；
- [x] 内置 Runtime；
- [x] 激活和姓名确认；
- [x] Credential Manager；
- [ ] 工作区选择；
- [x] Harness 生命周期和健康检查；
- [ ] 员工资料和额度展示；
- [ ] 手动和后台更新检查；
- [ ] 下载、校验、安装和回滚；
- [ ] 脱敏诊断包；
- [ ] 安装、升级和卸载体验；
- [x] 自包含安装源、Runtime 白名单、WebView2 条件安装和卸载数据策略；
- [ ] 安装包数字签名。

退出条件：普通员工无需命令行即可完成安装、激活和首个任务。

### M5：集成测试与试点

交付物：

- [ ] 全链路功能测试；
- [ ] Windows 10/11 x64 测试矩阵；
- [ ] 弱网、断网和网关故障测试；
- [ ] Key 泄露、吊销和额度测试；
- [ ] 升级中断和自动回滚测试；
- [ ] Harness 上游升级兼容性测试；
- [ ] 安装包安全扫描；
- [ ] 5 至 10 名真实员工试点；
- [ ] 收集并关闭阻塞问题；
- [ ] 完成用户和管理员操作手册。

退出条件：试点期间无高危安全问题、无数据丢失问题、无阻塞缺陷。

### M6：正式发布与运营

交付物：

- [ ] 正式签名安装包；
- [ ] 稳定更新通道；
- [ ] 网关和数据库生产部署；
- [ ] 监控、告警和备份；
- [ ] 支持流程和负责人；
- [ ] 分批推广计划；
- [ ] 月度用量与费用复盘；
- [ ] 上游版本定期检查；
- [ ] 季度权限和 Key 清理。

退出条件：项目转入常规运营和持续改进。

## 9. 工作项状态

所有工作项使用以下状态：

```text
Backlog → Ready → In Progress → Review → Verification → Done
                         └──────────────→ Blocked
```

状态定义：

| 状态 | 定义 |
|---|---|
| Backlog | 已记录但尚未完成范围和优先级评审。 |
| Ready | 需求、验收条件和依赖明确，可进入开发。 |
| In Progress | 正在设计、开发或配置。 |
| Review | 代码、设计或安全评审中。 |
| Verification | 已合并，正在集成测试或用户验收。 |
| Blocked | 存在明确外部依赖或阻塞条件。 |
| Done | 验收条件通过，文档和测试已同步。 |

## 10. Definition of Ready

工作项进入开发前必须满足：

- 业务目的明确；
- 范围和非范围明确；
- 用户行为和失败状态明确；
- 接口或数据变化明确；
- 安全与隐私影响已评估；
- 验收条件可验证；
- 依赖项已识别；
- 不要求未经批准的上游源码修改。

## 11. Definition of Done

工作项完成必须满足：

- 实现符合已确认需求；
- 单元测试和集成测试通过；
- 异常、超时和恢复路径已测试；
- 未引入明文密钥或内容日志；
- 相关文档已更新；
- 安装、升级和回滚影响已检查；
- 安全评审问题已关闭或正式接受；
- 可观察性指标已补充；
- 验收人确认通过。

## 12. 测试策略

### 12.1 客户端

- 全新安装；
- 覆盖升级；
- 卸载保留数据；
- 首次激活；
- 无效、过期、已使用激活码；
- 姓名确认取消；
- 重装后重新激活；
- 网关不可达；
- Harness 启动失败；
- 端口冲突；
- WebView2 缺失或损坏；
- 更新下载中断；
- 签名或哈希错误；
- 新版健康检查失败并回滚；
- 日志脱敏。

### 12.2 网关

- 普通和流式请求；
- Tool Calls；
- 无效、禁用、过期 Key；
- 模型越权；
- RPM、TPM、并发限制；
- 月度预算耗尽和重置；
- 上游超时、限流和故障；
- 客户端断开后取消请求；
- 用量统计准确性；
- 上游 Key 轮换；
- 确认请求与响应正文不进入日志。

### 12.3 管理服务

- 管理员登录、TOTP 和会话过期；
- 员工导入和重复数据；
- 激活码并发兑换；
- 员工停用联动 Key 禁用；
- LiteLLM 管理接口失败补偿；
- 权限越权；
- 审计日志完整性；
- 导出数据权限和脱敏。

### 12.4 Harness 上游兼容性

每个候选 Harness 版本至少验证：

- 启动和退出；
- 新建和恢复会话；
- 工作区读写；
- PowerShell 工具调用；
- 流式模型输出；
- Tool Calls；
- Skills 加载；
- 公司网关自定义 Provider；
- 配置和凭证路径；
- 旧用户数据兼容；
- 更新后回滚到上一稳定版。

## 13. 发布与版本策略

### 13.1 版本组成

发布清单分别记录：

```text
Desktop Shell：1.0.0
Harness Core：0.1.1-rc.2
Node Runtime：24.x.y
Company Skills：1.0.0
Gateway Config：1.0.0
```

### 13.2 发布通道

| 通道 | 用途 |
|---|---|
| candidate | 内部自动化测试和研发验证。 |
| pilot | 5 至 10 名员工灰度。 |
| stable | 公司正式发布。 |
| rollback | 当前稳定版的上一已验证版本。 |

### 13.3 发布门禁

- 精确依赖版本已锁定；
- 构建环境可复现；
- SBOM 和第三方许可证清单已生成；
- 安装包和更新包已签名；
- 哈希已发布；
- 自动化测试通过；
- 安全扫描无未接受的高危问题；
- 数据备份和回滚路径已验证；
- 更新说明已完成；
- 灰度结果已批准。

## 14. 上游 Harness 更新生命周期

```text
发现上游新版本
      ↓
登记候选版本和变更摘要
      ↓
依赖、许可证和安全检查
      ↓
构建公司候选 Runtime
      ↓
自动兼容性测试
      ↓
人工关键路径验证
      ↓
Pilot 灰度
      ↓
批准进入 Stable
      ↓
客户端提示更新
      ↓
监控失败率并保留回滚版本
```

禁止客户端直接执行 `npm update`、`npx ...@latest` 或直接下载未经公司签名的上游资产。

## 15. 安全与隐私验收

- [ ] 安装包中不存在上游真实 Key；
- [ ] Git 仓库中不存在真实 Key；
- [ ] 客户端日志不存在虚拟 Key；
- [ ] 网关日志不存在请求和回复正文；
- [x] 数据库只保存激活码哈希；
- [x] 虚拟 Key 可立即禁用；
- [ ] LiteLLM Master Key 不可由客户端访问；
- [ ] 管理后台启用 TOTP；
- [ ] 管理接口限制公网访问（当前测试期按要求临时开放 `8766`）；
- [ ] 所有正式环境通信使用 HTTPS（当前测试入口按决策暂用 HTTP）；
- [ ] 更新包必须验证签名和哈希；
- [ ] 公司配置与 Skills 包必须验证签名；
- [x] 用户停用可联动禁用全部 Key；
- [ ] 日志保留期限已定义；
- [ ] 数据备份可恢复。

### 15.1 禁止个人 Provider 的实施边界

公司桌面壳不提供个人 Provider 配置入口，并统一预置公司网关。若员工拥有本机管理员权限，仍可能手工修改本地 Harness 配置或使用其他工具直接访问外部模型 API。

需要强制禁止时，应额外使用公司 Windows 管理策略、AppLocker、文件 ACL、Windows Defender Firewall 或网络出口策略。桌面壳中的界面限制不能被视为强安全边界。

## 16. 运维要求

### 16.1 日常

- 网关可用性、延迟和错误率监控；
- PostgreSQL 容量和备份状态；
- 上游余额和限流状态；
- 激活失败和异常 Key 使用；
- 客户端版本分布；
- 强制更新完成率。

### 16.2 月度

- 员工、部门和模型费用复盘；
- 虚拟 Key 和离职员工清理；
- 模型价格表校验；
- 额度策略调整；
- 数据库恢复抽查；
- 上游 Harness 版本评估。

### 16.3 事件响应

真实上游 Key 泄露：

1. 立即在供应商侧轮换；
2. 更新网关密钥；
3. 排查日志和访问路径；
4. 验证客户端无真实 Key；
5. 登记安全事件和改进项。

员工虚拟 Key 泄露：

1. 禁用单个虚拟 Key；
2. 检查该 Key 的用量和来源；
3. 重新签发激活码或虚拟 Key；
4. 不需要轮换公司上游 Key。

客户端更新故障：

1. 暂停发布；
2. 将 Stable 指向上一验证版本；
3. 客户端执行自动回滚；
4. 保留脱敏诊断信息；
5. 修复后重新进入 Candidate。

## 17. 变更管理

范围、架构或安全边界发生变化时，新增变更记录：

| 字段 | 内容 |
|---|---|
| 变更编号 | CR-YYYY-NNN |
| 提出日期 | YYYY-MM-DD |
| 提出人 | 姓名 |
| 变更内容 | 具体描述 |
| 变更原因 | 业务或技术原因 |
| 影响范围 | 客户端、网关、管理端、数据、发布、运维 |
| 安全与隐私影响 | 无/有，详细说明 |
| 兼容与迁移 | 是否需要迁移或回滚 |
| 审批结果 | 待评审/批准/拒绝 |

批准的变更必须同步更新：

- 已确认决策；
- MVP 范围；
- 架构和接口；
- 测试计划；
- 发布和回滚计划。

## 18. 风险登记

| 编号 | 风险 | 影响 | 缓解措施 | 状态 |
|---|---|---|---|---|
| R-001 | Harness 处于快速迭代阶段，存在不兼容升级。 | 高 | 固定版本、候选验证、灰度、回滚。 | 持续跟踪 |
| R-002 | 本机管理员可以绕过客户端的个人 Provider 限制。 | 高 | Windows 管理和网络出口策略。 | 待确认终端管理条件 |
| R-003 | 上游 Key 泄露导致公司级费用风险。 | 高 | 仅服务器保存、密钥轮换、虚拟 Key。 | 已设计 |
| R-004 | 网关日志意外记录公司代码或提示词。 | 高 | 禁止正文日志、测试验证、日志脱敏。 | 已设计 |
| R-005 | 用量价格映射不准确导致预算误差。 | 中 | 公司维护模型价格表并定期核对。 | 已设计 |
| R-006 | 激活码被转发给他人。 | 中 | 短有效期、一次性、显示姓名确认、尝试限流。 | 已设计 |
| R-007 | 客户端更新失败导致不可用。 | 高 | 原子替换、健康检查、自动回滚。 | 已设计 |
| R-008 | LiteLLM 或数据库单点故障。 | 高 | 备份、监控、恢复演练，后续按规模高可用。 | 待实施 |
| R-009 | 第三方许可证不符合公司政策。 | 中 | SBOM、许可证扫描和法务评审。 | 待评审 |
| R-010 | Windows WebView2 或安全软件阻止运行。 | 中 | 安装检测、签名、试点兼容性测试。 | 待验证 |

## 19. 项目度量

项目质量至少跟踪：

- 安装成功率；
- 首次激活成功率；
- Harness 启动成功率；
- 更新成功率和回滚率；
- 网关请求成功率；
- P50/P95 首 Token 延迟；
- RPM/TPM/预算拒绝次数；
- 虚拟 Key 泄露或异常使用次数；
- 客户端崩溃率；
- 当前稳定版本覆盖率；
- 高危缺陷平均关闭时间。

这些指标不得通过采集提示词、代码或回复正文实现。

## 20. 退役与数据处理

员工离职：

- 禁用员工账号；
- 禁用所有虚拟 Key；
- 使未使用激活码失效；
- 按公司终端和代码资产政策处理本地数据；
- 保留必要的用量和管理员审计记录。

产品退役：

- 停止发布新客户端；
- 通知员工截止时间；
- 禁用更新和激活入口；
- 吊销全部虚拟 Key；
- 轮换上游 Key；
- 导出必要审计记录；
- 按保留策略删除员工和用量数据；
- 归档源码、构建材料、SBOM、许可证和最终版本。

## 21. 下一步执行顺序

1. [x] 建立仓库目录与模块边界；
2. [x] 编写威胁模型；
3. [x] 完成管理服务数据库和接口设计；
4. [x] 搭建 LiteLLM + PostgreSQL 技术验证环境；
5. [x] 验证 Harness 聊天与原生联网搜索通过公司 LiteLLM；
6. [x] 验证 Windows 本地固定 Runtime 启动 Harness；
7. [x] 实现桌面壳、WebView2 与激活端到端最小闭环；
8. [ ] 实现额度、停用和用量统计；
9. [ ] 实现更新、签名与回滚；
10. [ ] 完成公司 Skills 分发；
11. [ ] 进入试点和正式发布阶段。

## 22. 变更记录

| 日期 | 版本 | 说明 |
|---|---|---|
| 2026-09-03 | 0.1 | 创建项目生命周期基线，记录已确认需求、MVP、里程碑、发布和运维流程。 |
| 2026-09-03 | 0.2 | 启动第一阶段开发：建立解决方案、激活闭环、LiteLLM 签发适配器、Windows Runtime 核心、更新校验、部署与测试基线。 |
| 2026-09-03 | 0.3 | 建立远端 Docker 技术验证环境，同步项目、生成测试环境密钥并启动 LiteLLM 镜像拉取。 |
| 2026-09-03 | 0.4 | 生成新版公司 Logo 资产，放宽 Visual Studio 的 .NET 8 SDK feature band，并将网关宿主机测试端口调整为 8765。 |
| 2026-09-04 | 0.5 | 完成远端网关 14 项自动化验收；确认流式、Tool Calls、虚拟 Key、模型权限、四类额度和正文隐私检查全部通过。 |
| 2026-09-04 | 0.6 | 验证 Harness 原生 DeepSeek 联网搜索可通过 LiteLLM `/v1/messages` 转发；保留内置插件并统一接入公司网关。 |
| 2026-09-04 | 0.7 | 完成 Windows Harness Runtime 第一轮集成：固定 Node.js 24.20.0 与 Harness 0.1.1-rc.2、锁定完整依赖、加入公司策略覆盖、隔离个人 Provider 环境变量，并通过真实聊天、联网搜索和本地 Web 启动测试。 |
| 2026-09-04 | 0.8 | 完成 Windows 桌面壳首版：公司品牌界面、激活预览与员工身份确认、Credential Manager、Runtime 定位和 WebView2 容器；真实 EXE 完成错误态及身份页视觉验收。 |
| 2026-09-04 | 0.9 | 完成真实激活闭环：LiteLLM 员工虚拟 Key 签发、公司模型鉴权、Windows 隔离凭据、Harness 冷启动、WebView2 加载与进程树关闭；修复冷启动健康探测误判。 |
| 2026-09-04 | 0.10 | 将控制面编译产物部署到云服务器；`8765` 切换为 HTTPS 统一入口，隐藏 LiteLLM 管理 API；客户端加入证书固定校验及 Node 额外 CA 信任，并完成远端直连验收。 |
| 2026-09-04 | 0.11 | 激活服务切换到独立 PostgreSQL 数据库和 Production 模式；完成幂等迁移、哈希存储、行锁单次兑换、重启持久化、并发确认及验收 Key 清理。 |
| 2026-09-04 | 0.12 | ControlPlane 迁移为 Java 21 + Spring Boot 4.1.1，并新增 Vue 管理网页；Nginx 分离公网 8765 与回环管理 8766，完成 CSV、批量激活、数据兼容、模型鉴权和真实浏览器验收后移除旧 ASP.NET 架构。 |
| 2026-09-05 | 0.13 | 按测试要求将管理端口 `8766` 临时改为公网直连，并为 `/admin` 增加规范化跳转；正式试点前仍须恢复来源限制和 HTTPS。 |
| 2026-09-05 | 0.14 | 新增员工单人添加、受限删除、启用和禁用；禁用联动撤销 LiteLLM Key 与未使用激活码，完成自动化 API 闭环及真实浏览器验收。 |
| 2026-09-05 | 0.15 | 修复员工额度整数溢出被误报为 HTTP 500；前后端统一月额度、RPM、TPM 和并发上限，越界请求改为可操作的 HTTP 400 中文提示。 |
| 2026-09-06 | 0.16 | 分离激活码与员工访问期限：激活码增加六档预设及最长 90 天自定义时间，员工支持长期或指定期限；新增到期自动禁用、LiteLLM Key/未用码撤销、CSV 兼容字段和管理端期限编辑，并完成远端真实到期验收。 |
| 2026-09-06 | 0.17 | Windows 桌面壳改为 48px 单层标题栏，保留原生窗口行为；新增员工菜单、Harness/Windows/固定浅深四档外观策略、零侵入 WebView2 主题桥和偏好持久化测试。 |
| 2026-09-06 | 0.18 | 实现每用户单实例唤醒、自包含 Windows x64 安装源、Inno Setup 定义、Runtime 白名单打包、微软 WebView2 签名校验和可选卸载数据清理；测试增至 18 项。 |
| 2026-09-09 | 0.19 | 使用 Inno Setup 7.1.0 完成 0.1.0 Windows x64 安装包首次完整构建；修正最低 Windows 版本语法和卸载清理一次性标识，生成约 96 MB 安装包及匹配的 SHA-256 元数据清单。 |
| 2026-09-09 | 0.20 | 模型选择切换为 DeepSeek 官方 ID；新增 PostgreSQL 服务端模型目录、管理网页启停/默认配置、活跃虚拟 Key 权限同步、客户端启动时动态刷新与离线缓存，并保留旧公司别名作 0.1.x 迁移兼容。当前启用 V4 Flash、V4 Pro 与 V4 Flash Vision Exp；未加入尚无稳定官方 API ID 的 4.1 预览项。 |
| 2026-09-09 | 0.21 | 发布 Windows 客户端 0.2.1 热修复：修正动态模型目录 YAML 中 `models:` 后缺少换行导致 Harness 启动退出的问题；增加精确格式回归测试并完成真实安装、配置生成、Harness 启动和卸载闭环验证。 |
| 2026-09-10 | 0.22 | 修复安装后首次启动竞态及同进程重试失效：安装器向客户端传递自身进程并由客户端等待收尾；启动最多自动重试三次，每次均终止旧 Harness、释放失败的 WebView2 并新建干净实例；最终失败后仍可直接手动重试。 |
| 2026-09-10 | 0.23 | 根据 0.2.2 现场复现确认 Harness Runtime 与动态配置可正常独立启动，残余故障位于首次安装宿主进程内的 WebView2 初始化；增加一次受控跨进程恢复，恢复进程等待旧实例退出后再接管且禁止自动循环；同时增加脱敏启动日志、诊断编号和诊断目录入口。 |
| 2026-09-10 | 0.24 | 依据诊断编号 F6F678BE8E 修正真实根因：Harness 用户 Profile 的模块回退链接在 Runtime 安装/更新后首次加载会触发 `ERR_MODULE_NOT_FOUND`。正式 Web 服务前新增独立官方 `--dump-default-config` 预检进程，先初始化并重新指向依赖链接；同时移除 WPF `Closing` 的异步取消模式，改为同步终止本地进程树并交还 Windows 原生关闭流程。完成旧 Runtime 链接迁移及连续三轮启动/关闭验证。 |
| 2026-09-10 | 0.25 | 依据诊断编号 D02B1E9A4C 加固模块解析：官方 Profile 预检后为 `profiles/web/node_modules` 建立指向当前固定 Runtime 的直接 Junction，并在正式启动前执行 ESM 依赖探针；升级时自动重定向旧桥接，不修改上游 Harness 源码、不复制依赖，辅助进程不接收虚拟 Key。按 HTTP 200、持续存活和进程树退出标准完成连续三轮及旧 Runtime 迁移验收。 |
| 2026-09-10 | 0.26 | 依据诊断编号 AD45EB0FBD 移除产生瞬时误判的 ESM `eval` 探针及多次自动/跨进程恢复；改为桌面端直接验证 Runtime 必要文件、同步桥接后仅启动一次正式 Harness，并以正式 HTTP 健康检查判定。安装 Runtime 改为 `runtime-<客户端版本>` 独立目录，避免升级覆盖同一路径。完成真实安装首次启动、旧桥接迁移、单子进程、连续 HTTP 200、零新增诊断和退出清理验收。 |
| 2026-09-10 | 0.27 | 依据 0.2.6 安装完成页日志确认 Windows Junction 在安装后的首个正式 Node 进程中仍存在解析时序问题。撤销 `profiles/web/node_modules` 桥接，新增通过 Node `--import` 注册的受限模块解析钩子：标准解析失败时仅回退到当前版本化 Runtime，且真实路径不得越界。完成无 Profile 链接的独立 Harness HTTP 200 验证及真实安装故障注入，单进程连续三次 HTTP 200、零新增诊断、退出无残留。 |
| 2026-09-10 | 0.28 | 将项目定位调整为可供不同企业定制部署的 DeepSeek Harness 企业版方案；补充服务端部署、Windows 构建、企业定制和 GitHub Releases 发布流程。源码仓库不保存 EXE、JAR、前端 `dist`、固定 Runtime 等构建产物，安装包与哈希元数据仅通过版本 Release 分发。 |
| 2026-09-14 | 0.29 | 将真实企业品牌替换为虚构示例“超智能战斗轮椅有限公司”；新增集中品牌模板和 Logo 重建脚本，统一桌面端、管理端、安装器、Java 包名、激活码前缀及文档，并通过独立安装身份和数据目录隔离原企业版本。 |
| 2026-09-14 | 0.30 | 将示例品牌管理网页与 `com.smartwheelchair.harness` ControlPlane 部署到远端测试环境；保留 PostgreSQL、LiteLLM、员工及激活数据，建立品牌中立运维入口和部署前备份。公网 8765/8766、数据库健康、管理员 API、静态资源与运行 JAR 哈希均验收通过。 |
| 2026-09-14 | 0.31 | 准备无旧 Git 历史的开源发布快照：移除真实服务器地址、SSH 目标、备份标识和私有许可证声明，将公网地址改为环境配置；新增安全策略、贡献指南、开源检查清单和自动发布扫描。现有私有仓库与服务器部署保持不变。 |
| 2026-09-14 | 0.32 | 采用 Apache License 2.0 作为项目开源许可证，补充 NOTICE、第三方许可证与商标边界；新建无旧历史的同名 GitHub 仓库，并将旧私有仓库隔离为 `deepseek-harness-enterprise-old`。 |
